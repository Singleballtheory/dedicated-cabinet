import React from "react";

function Header(){
  const headerStyle = {
    textAlign: "center",
    color: "purple"
  }
  return (
    <h1 style={headerStyle}>Dedicated Cabinet</h1>
  );
}

export default Header;